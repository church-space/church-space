import React from "react";

export default function page() {
  return (
    <div>
      Import Page. upload CSV, make sure that the column is marked as Email
      Address, and then say if these contacts should be marked as unsubscribed
      or cleaned
    </div>
  );
}
